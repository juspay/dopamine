# Python dependencies for the Instagram collection scripts (scripts/*.py).
# Install with: pip install -r scripts/requirements.txt
instagrapi>=2.0
python-dotenv>=1.0
browser-cookie3>=0.19
# Fallback saved-posts backend (IG_COLLECTOR=gallerydl) — cookie-auth web scraper.
gallery-dl>=1.32
